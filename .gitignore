# dependencies
node_modules

# build output
dist
dist-ssr
build
.next
.vite
*.tsbuildinfo

# local environment (keys) — .env.example is committed instead
.env
.env.local
.env*.local
.env.*
!.env.example
*.local

# deploy / OS / editor
.vercel
.DS_Store
.claude/settings.local.json

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
