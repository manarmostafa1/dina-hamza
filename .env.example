# Web3Forms access key for the contact postcard.
# Get one free at https://web3forms.com (enter the email that should receive messages).
# Local: put the real key in .env.  Production: add it in your host's environment variables.
VITE_WEB3FORMS_KEY=
